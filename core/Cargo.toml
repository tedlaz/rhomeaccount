[package]
name = "rhomeaccount-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Core accounting library for rhomeaccount (port of qlogistiki)"

[dependencies]
chrono = "0.4"

[dev-dependencies]
serde_json = "1"
